# 345. Reverse Vowels of a String 

## My solution(solution referenced)
```java
class Solution {
    public String reverseVowels(String s) {

        //Convert the string to Array
        char[] chars = s.toCharArray(); 

        //Declare the String 
        int left = 0; 
        int right = s.length() - 1; 

        //Use while loop until to characters meet. 
        while (left < right){

            //If chars[left] is not vowel, keep moving to right. 
            while (left < right && !isVowel(chars[left])){
                left++; 
            }

            //If chars[right] is not vowel, keep moving to left. 
            while (left < right && !isVowel(chars[right])){
                right--; 
            }

            //Swap the vowels. 
            if(left < right){
                char temp = chars[left]; 
                chars[left++] = chars[right]; 
                chars[right--] = temp; 
            }
        }

        return new String(chars);

    }

    //Return true when char is vowel. 
    private boolean isVowel(char c){
        return c == 'a' || c == 'e' || c == 'i' || c == 'o' 
        || c == 'u' || c == 'A' || c == 'E' || c == 'I' || c == 'O' || c == 'U' ; 
    }
}
```
## What I learned 
toCharArray: Returns a copy of the input data.
- Returns: an array of chars copied from the input data.
- (e.g.) 
```java
class Gfg {
    public static void main(String args[])
    {
        String s = "GeeksForGeeks";
        char[] gfg = s.toCharArray();
 
        System.out.println(gfg); 
        // output = GeeksForGeeks 
    }
}
```
